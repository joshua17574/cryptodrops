# Database Configuration
# For production, use a secure database with TLS/SSL enabled
# Example: postgresql://username:password@host:5432/database_name
DATABASE_URL=postgresql://username:password@localhost:5432/database_name

# JWT Secret (REQUIRED - use a strong random string)
# Generate using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# IMPORTANT: NEVER commit your actual JWT_SECRET to version control
JWT_SECRET=your-secret-jwt-key-here

# Admin Setup (used by setup-admin.js)
# These credentials are used to create the initial admin user
# Use strong passwords (min 12 characters, mixed case, numbers, symbols)
ADMIN_USERNAME=your_admin_username
ADMIN_PASSWORD=your_secure_password

# Node Environment (development | production)
NODE_ENV=development

# Email Configuration (Optional - for newsletter functionality)
# Sign up at https://resend.com to get your API key
RESEND_API_KEY=your_resend_api_key_here
RESEND_FROM_EMAIL=noreply@yourdomain.com

# CORS Configuration (Required for production)
# Comma-separated list of allowed origins for API access
# Example: ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
ALLOWED_ORIGINS=

# Security Settings
# Set to true ONLY in development if you encounter SSL/TLS certificate issues
# NEVER set this to true in production
DISABLE_TLS_VERIFY=false

# Rate Limiting (Optional - customize as needed)
# AUTH_RATE_LIMIT_WINDOW_MS=900000
# AUTH_RATE_LIMIT_MAX_REQUESTS=5
